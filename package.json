{
  "name": "mcpcli",
  "version": "0.1.0",
  "description": "CLI for interacting with MCP servers — TypeScript alternative to mcptools",
  "type": "module",
  "bin": { "mcpcli": "dist/index.js" },
  "main": "dist/index.js",
  "files": ["dist/"],
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": ["mcp", "model-context-protocol", "cli", "tools", "ai", "llm"],
  "license": "MIT"
}
